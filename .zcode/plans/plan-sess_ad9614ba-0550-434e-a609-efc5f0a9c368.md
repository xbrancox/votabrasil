# v27 — Prints do f4 + Radar Político real (reclamação/apoio/resposta/verificação) + Fundo Eleitoral (TSE)

## Contexto

Pedido (print anotado da tela "Voto registrado" + 2 itens novos):
1. **Prints**: remover "↗ Compartilhar" do f4; remover o acesso redundante a "Conferir" na barra inferior; esclarecer na UI a diferença demonstração × votação real.
2. **Radar Político (app)**: substituir a lista fake hardcoded por políticos REAIS do backend, com reclamação, apoio, respostas e selo verificado/não verificado + link de verificação.
3. **Fundo eleitoral (dados públicos TSE)**: exibir no app E nas páginas web, por partido e por político/candidato que recebeu.

Perguntas de clarificação enviadas sem resposta → melhor julgamento (opções recomendadas): remove-se o ícone "Conferir" da barra inferior (fica acessível pelo "🔍 Conferir no site" do f4); fundo eleitoral em seção dedicada + bloco na ficha do político.

Descobertas-chave:
- Backend já tem tudo para o item 2: `GET /api/candidatos` (594 reais, com `selo/verificado`), `POST /api/reclamacoes/public` (anônimo, `{politicianId, tipo:'apoio'|'rec', content}`), `GET /api/reclamacoes|/api/apoios?politicianId=`, `GET /api/estatisticas/politico/{pid}`, `GET /api/rankings`, fluxo `/api/verificacao/dominios|iniciar|confirmar`.
- Radar atual = mock hardcoded (app/index.html:573–577) com selo "Verificado" falso.
- **Nenhum dado de fundo eleitoral existe no repo** (grep confirmado). Fonte oficial: TSE Dados Abertos (FEFC por partido + receitas de prestação de contas por candidato + Fundo Partidário anual).
- app/app.js é código morto; tudo vive inline em app/index.html. Anti-cache: sw.js v26→v27. Web: home SPA index.html + pages/*.html + nav js/site-header.js.

## 1 — Prints (app/index.html)

1. Remover linha 265 (botão Compartilhar) e a função `compartilharCodigo()` (507–513).
2. Barra inferior: remover botão `data-s="s-conf"` "Conferir" (linha 355); Conferir segue acessível via "🔍 Conferir no site" (f4) e links internos. Manter pílula flutuante #pil. Ajustar CSS .nv se necessário.
3. Demonstração × votação real: bloco对比 claro na tela de explicação ("🧪 DEMONSTRAÇÃO hoje: voto de teste que alimenta a pesquisa, pode ser zerado/refeito, sem valor legal" vs "🗳️ VOTAÇÃO REAL amanhã: login por e-mail/telefone, comprovante automático, voto não apagável, vale para revogação"); rodapé do f4 com frase explicativa; revisar ocorrências da palavra (linhas 232/248/266/521) para uso consistente com explicação no primeiro uso.

## 2 — Radar Político real (app/index.html)

- Boot lazy ao abrir #s-rad: `GET /api/candidatos` → cards reais: foto (fallback iniciais), nome, partido·UF·cargo, selo ✅ Verificado / ⚪ Não verificado. Busca #q filtra client-side (nome/partido/UF). Ordenar verificados + mais reclamados (/api/rankings); render 50 + "carregar mais".
- Accordion por card: **Reclamar** (textarea, POST /api/reclamacoes/public tipo 'rec', min 10 chars validado antes), **Apoiar** (tipo 'apoio'), **Ver tudo** (GET reclamacoes+apoios do político, com respostas quando houver `response`). Contadores via /api/estatisticas/politico/{pid} sob demanda.
- Não verificado → "**É você? Verifique seu mandato →**": input e-mail institucional (dica com domínios de /api/verificacao/dominios) → POST /api/verificacao/iniciar → toast "link enviado ao seu e-mail" (mostrar devConfirmUrl só se presente).
- Erro de rede → toast + retry; nunca dados fake. `opinar` permanece (usado no Congresso hardcoded, fora deste pedido).

## 3 — Fundo Eleitoral (TSE)

### server/fundo-eleitoral.js (novo, padrão ingest.js)
- Cache `data/fundo-eleitoral.json`, TTL semanal, force refresh, fallback ao cache antigo se fonte cair. Fontes: distribuição do FEFC por partido, receitas eleitorais por candidato (prestação de contas), Fundo Partidário anual — dadosabertos.tse.jus.br.
- Schema: `porPartido:[{sigla,ano,fefcTotal,fundoPartidario}]`, `porPolitico:[{nome,cargo,uf,siglaPartido,ano,valorRecebido,origem}]`; match com candidatos via nome normalizado+partido+UF (heurística documentada; sem match → "não localizado na fonte TSE"). Se a rede do Railway bloquear a URL, commitar snapshot real gerado localmente agora (fonte citada + link). **Dados reais sempre.**

### Endpoints (server/index.js)
- `GET /api/fundo-eleitoral/partidos?ano=` · `GET /api/fundo-eleitoral/politico?q=` · `GET /api/fundo-eleitoral/resumo` (totais, atualização, fonte).

### App celular
- Nova seção `#s-fundo` ("Fundo Eleitoral") com resumo + tabs **Por partido** (ranking com barras proporcionais) | **Buscar político**; expandir partido mostra maiores beneficiários; link "Fonte: TSE Dados Abertos ↗". Entrada por card na home + item "Fundo" na barra inferior (slot liberado por Conferir → 6 itens: Início, Votar, Radar, Fundo, Congresso, Revogar).
- Ficha do político no Radar ganha linha "💰 Fundo eleitoral recebido: R$ X (ano)" quando houver match.

### Web
- `pages/fundo-eleitoral.html` nova (layout de parlamentares.html + site-header.js); item "Fundo Eleitoral" no array ITENS de js/site-header.js; card na vitrine da home index.html.

## 4 — Versão
sw.js CACHE v26→v27; bumps ?v= correspondentes. Regenerar templates/cedula-votabrasil.html (f4 mudou) para manter o pacote sincronizado.

## Teste & deploy
1. `node --check` nos JS tocados; servidor local + curl nos endpoints novos e no fluxo de reclamação pública.
2. Browser Use 390×844 (local → produção): f4 sem Compartilhar; barra sem Conferir; Radar com políticos reais; buscar "Arthur", reclamar/apoiar e confirmar persistência; link de verificação valida domínio; tela Fundo carrega ranking/busca.
3. Commit+push (nunca versionar server/data/* sensíveis; data/fundo-eleitoral.json é público e pode ser versionado) → `railway up --service mudabrasil-redesign` → aguardar SUCCESS.
4. Smoke final em produção + relatório em português com sugestões da fase real.

## Arquivos tocados
- app/index.html · app/sw.js · server/fundo-eleitoral.js (novo) · server/index.js · data/fundo-eleitoral.json (novo) · pages/fundo-eleitoral.html (novo) · js/site-header.js · index.html · templates/cedula-votabrasil.html